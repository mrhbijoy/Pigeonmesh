# Build output. build/build.sh writes packages to ../dist and stages in a
# temporary directory outside the tree, so there is nothing else to ignore
# here -- build/ itself is source.
dist/

# Locally built apk-tools, if you keep one here to get "apk mkpkg".
.tools/
